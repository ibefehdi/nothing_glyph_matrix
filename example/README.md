# nothing_glyph_matrix example

Demo app for the Glyph Matrix plugin.

1. Put `glyph-matrix-sdk.jar` (extracted from Nothing's AAR) in `../android/libs/` — see parent README.
2. `flutter run` on a Nothing Phone (3) (or any device — matrix calls no-op safely).
