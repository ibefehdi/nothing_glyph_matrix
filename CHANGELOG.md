## 0.1.0

* Initial release: `GlyphMatrix` MethodChannel client and `GlyphMatrixDisplay`
  priority helper (peek / rest / progress / idle image / celebration).
* Android plugin wrapping Nothing's Glyph Matrix SDK (`setAppMatrixFrame`).
* Does **not** redistribute Nothing's proprietary AAR — see `android/libs/README.md`.
